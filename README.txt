test1.py 파일을 실행시 내가 설정한 json 파일을 가져와 파싱한뒤 그 값을 requirements.txt 로 만든다.

test1.json 이나 data.json 을 예시로 참고해보면 될듯합니다.

자세한 코드는 test1.py 파일에 주석을 달아두었으니 확인해주시기 바랍니다.

참고로 웹에서 들어온 json 데이터를 바로 파싱하려면 파일을 설정하는게 아닌 웹에서 들어온 데이터를
가져올수있게 수정해줘야합니다.

수정할 위치도 주석에 표기했습니다.
